{
  "name": "API",
  "version": "1.0.0",
  "description": "API Test Automation",
  "scripts": {
    "test:mainnet": "npx playwright test --browser=webkit --config=prod.config.ts --workers=5",
    "test:debug": "npx playwright test --headed",
    "test:chromium": "npx playwright test --browser=chromium",
    "test:firefox": "npx playwright test --browser=firefox",
    "test:webkit:headed": "npx playwright test --browser=webkit --config=prod.config.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/katanasushi/upgraded-doodle.git"
  },
  "author": "QA",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.23.2"
  },
  "devDependencies": {
    "cz-conventional-changelog": "^3.3.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
